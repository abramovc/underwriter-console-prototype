# iq.dev

Source of truth for provider mappings:
`canonical-schema/mappings/*.csv`

Generated mapping artifact:
`underwriter-console-prototype/data/provider-mappings.json`

Regenerate mappings from the repo root with:
`npm run export:mappings`
